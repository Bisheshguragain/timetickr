
// This file is a central place to export Firebase related types.
// Initialization is handled by the `useFirebase` hook to ensure
// it only runs on the client-side.

export type { FirebaseServices } from './firebase-types';
