
"use client";

// This hook is no longer used and can be removed.
// The new pattern in `src/lib/firebase.ts` provides a cleaner,
// more reliable way to initialize and access Firebase services.

export function useFirebase() {
    return null;
}
